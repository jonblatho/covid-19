name: Auto-Archive HCHD Dashboard

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 13 * * *" # Run at 8 AM CDT and retries at 8:30 in case the job fails

jobs:
  archive:
    runs-on: ubuntu-20.04
    
    steps:
    - uses: JamieMagee/wayback@v1.2.16
      with:
        url: https://allthingsmissouri.org/county-covid-19-dashboards/howell-county/
        saveErrors: false
        saveOutlinks: true
        saveScreenshot: false